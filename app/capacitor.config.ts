import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rutastampico.mafa',
  appName: 'Rutas Tampico MAFA',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
